# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Cached real-data snapshots and scratch run outputs (regenerate with --real).
# Snapshots hold raw vendor price/yield data — keep the mechanism, not the data.
# NOTE: anchored to the repo root so it does NOT match the taa/data/ package.
*.pkl
*_out.txt
*_err.txt
final_tests.txt
/data/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
